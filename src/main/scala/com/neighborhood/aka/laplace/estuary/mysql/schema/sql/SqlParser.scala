package com.neighborhood.aka.laplace.estuary.mysql.schema.sql

/**
  * Created by john_liu on 2018/6/8.
  * 我们sql使用maxwell的sql parser
  *
  *  www.maxwell.io
  */
object SqlParser {

//  def parseAlterTable(alterSql: String)(implicit currentDb: String): List[TableAlter] = {
//    parse(alterSql, currentDb).map(_.asInstanceOf[TableAlter])
//  }
//
//  def parseCreateTable(createSql: String)(implicit currentDb: String): List[TableCreate] = {
//    parse(createSql, currentDb).map(_.asInstanceOf[TableCreate])
//  }
//
//
// def parse(sql: String, currentDb: String): List[SchemaChange] = {
//    import scala.collection.JavaConverters._
//    SchemaChange.parse(currentDb, sql).asScala.toList
//  }

}
